.DS_Store
.env
.env.*
!.env.example
.venv/
__pycache__/
build/
# Where recordings are staged for the pipeline, one directory per call in
# flight. Cleared after each call unless KEEP_WORKDIR is set.
workdir/
data/
backups/

# Real customer call recordings, their transcripts and the audits drawn from
# them -- ~900 MB, and named customers on tape. The replay tools read it from a
# developer machine; it must never reach a git remote, where it cannot be
# unpublished.
files/
